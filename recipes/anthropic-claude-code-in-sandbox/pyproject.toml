[project]
name = "anthropic-claude-code-in-sandbox"
version = "0.1.0"
description = "Run Anthropic Claude Code in an Islo sandbox"
requires-python = ">=3.10"
dependencies = [
  "islo>=0.3.1",
  "python-dotenv>=1.0",
]
