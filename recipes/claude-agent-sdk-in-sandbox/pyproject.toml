[project]
name = "claude-agent-sdk-in-sandbox"
version = "0.1.0"
description = "Run the Claude Agent SDK in Islo"
requires-python = ">=3.10"
dependencies = [
  "islo>=0.3.1",
  "python-dotenv>=1.0",
]
