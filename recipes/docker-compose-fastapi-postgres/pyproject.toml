[project]
name = "docker-compose-fastapi-postgres"
version = "0.1.0"
description = "FastAPI + Postgres stack with Docker Compose on Islo"
requires-python = ">=3.10"
dependencies = [
  "islo>=0.3.1",
  "python-dotenv>=1.0",
]
